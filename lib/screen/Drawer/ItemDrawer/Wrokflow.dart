import 'package:flutter/material.dart';

class WrokflowScreen extends StatelessWidget {
  const WrokflowScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Wrokflow"),
        centerTitle: true,
        backgroundColor: Colors.amber,
      ),
    );
  }
}
