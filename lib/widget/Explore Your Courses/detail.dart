import 'package:flutter/material.dart';

class DetailExplore extends StatelessWidget {
  const DetailExplore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
